import React from "react"
import type { ModalConfig } from "@/redux/features/modalSlice"

const DefaultModal = (config: ModalConfig) => {
  return <div>DefaultModal</div>
}

export default DefaultModal
